[package]
name = "challenge-pot-server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8"
common = { path = "../common" }
serde = "1.0.228"
serde_json = "1.0.150"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["cors"] }
